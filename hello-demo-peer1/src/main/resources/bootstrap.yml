spring:
  cloud:
    config:
      ## 名字是为了更好的演示  事实上，建议配置文件名为我们的服务名
      name: helloserver
      uri: http://localhost:5001/
  profiles:
    active: dev

#  security:
#    user:
#      name: user
#      password: 123456